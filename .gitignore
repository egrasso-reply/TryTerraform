# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Valori reali degli studenti: si committano solo i *.tfvars.example
# (questi pattern non toccano *.tfvars.example, che non finisce per .tfvars)
*.tfvars
*.tfvars.json
